# Multi-stage Docker build: separate test and production targets.
# Same reusable workflow as push.yml, just with two extra inputs.
name: Keboola Component Build & Deploy Pipeline
on:
  push:
    branches-ignore:
      - main
    tags:
      - "*"

concurrency: ci-${{ github.ref }}

jobs:
  ci:
    uses: keboola/component-ci/.github/workflows/component-pipeline.yml@master
    with:
      app_id: "kds-team.ex-google-my-business"
      vendor: "kds-team"
      test_build_target: "test"
      docker_build_target: "production"
    secrets: inherit
